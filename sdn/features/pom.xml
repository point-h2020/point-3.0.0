<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>eu.point</groupId>
        <artifactId>commons</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../commons/parent</relativePath>
    </parent>
    <groupId>eu.point</groupId>
    <artifactId>features</artifactId>
    <name>Project features</name>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <properties>
        <features.file>features.xml</features.file>
    </properties>

   <dependencyManagement>
    <dependencies>
      <!-- project specific dependencies -->
      <dependency>
        <groupId>org.opendaylight.controller</groupId>
        <artifactId>mdsal-artifacts</artifactId>
        <version>${feature.mdsal.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.opendaylight.netconf</groupId>
        <artifactId>restconf-artifacts</artifactId>
        <version>${feature.restconf.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.opendaylight.yangtools</groupId>
        <artifactId>yangtools-artifacts</artifactId>
        <version>${yangtools.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

    <dependencies>

        <!-- External features that will be referenced -->
        <dependency>
            <groupId>org.opendaylight.netconf</groupId>
            <artifactId>features-restconf</artifactId>
            <version>${feature.restconf.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.opendaylight.controller</groupId>
            <artifactId>features-mdsal</artifactId>
            <version>${feature.mdsal.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.opendaylight.openflowplugin</groupId>
            <artifactId>features-openflowplugin</artifactId>
            <version>${feature.openflowplugin.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.opendaylight.netconf</groupId>
            <artifactId>features-netconf-connector</artifactId>
            <version>${feature.netconf.connector.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.opendaylight.yangtools</groupId>
            <artifactId>features-yangtools</artifactId>
            <classifier>features</classifier>
            <version>${yangtools.version}</version>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>
        <dependency>
      		<groupId>org.opendaylight.mdsal.model</groupId>
      		<artifactId>features-mdsal-model</artifactId>
      		<version>${maven-sal-api-gen-plugin.version}</version>
      		<classifier>features</classifier>
      		<type>xml</type>
      		<scope>runtime</scope>
        </dependency>
        <dependency>
      		<groupId>org.opendaylight.dlux</groupId>
     		<artifactId>features-dlux</artifactId>
      		<classifier>features</classifier>
      		<version>${feature.dlux.version}</version>
      		<type>xml</type>
      		<scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.opendaylight.l2switch</groupId>
            <artifactId>features-l2switch</artifactId>
            <classifier>features</classifier>
            <version>${feature.l2switch.version}</version>
            <type>xml</type>
            <scope>runtime</scope>
        </dependency>

         <!-- Bootstrapping specific artifacts -->
         <dependency>
            <groupId>eu.point</groupId>
            <artifactId>bootstrapping-config</artifactId>
            <version>${bootstrapping.version}</version>
            <type>xml</type>
            <classifier>config</classifier>
         </dependency>
         <dependency>
            <groupId>eu.point</groupId>
            <artifactId>bootstrapping-impl</artifactId>
            <version>${bootstrapping.version}</version>
          </dependency>
          <dependency>
            <groupId>eu.point</groupId>
            <artifactId>bootstrapping-model</artifactId>
            <version>${bootstrapping.version}</version>
        </dependency>
        
         <!-- Monitoring specific artifacts -->
         <dependency>
            <groupId>eu.point</groupId>
            <artifactId>monitoring-config</artifactId>
            <version>${monitoring.version}</version>
            <type>xml</type>
            <classifier>config</classifier>
         </dependency>
         <dependency>
            <groupId>eu.point</groupId>
            <artifactId>monitoring-impl</artifactId>
            <version>${monitoring.version}</version>
          </dependency>
          <dependency>
            <groupId>eu.point</groupId>
            <artifactId>monitoring-model</artifactId>
            <version>${monitoring.version}</version>
        </dependency>

        <!-- Registry specific artifacts -->
        <dependency>
            <groupId>eu.point</groupId>
            <artifactId>registry-config</artifactId>
            <version>${registry.version}</version>
            <type>xml</type>
            <classifier>config</classifier>
        </dependency>
        <dependency>
            <groupId>eu.point</groupId>
            <artifactId>registry-impl</artifactId>
            <version>${registry.version}</version>
        </dependency>
        <dependency>
            <groupId>eu.point</groupId>
            <artifactId>registry-model</artifactId>
            <version>${registry.version}</version>
        </dependency>
        
         <!-- UI specific artifacts -->
         <dependency>
            <groupId>eu.point</groupId>
            <artifactId>icnsdnui-bundle</artifactId>
            <version>${icnsdnui.version}</version>
         </dependency>

        <!-- tmsdn specific artifacts -->
        <dependency>
            <groupId>eu.point</groupId>
            <artifactId>tmsdn</artifactId>
            <version>${tmsdn.version}</version>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-handler</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-codec</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
            <version>${protobuf.version}</version>
        </dependency>
    </dependencies>
     <build>
        <resources>
            <resource>
                <filtering>true</filtering>
                <directory>src/main/features</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>filter</id>
                        <goals>
                            <goal>resources</goal>
                        </goals>
                        <phase>generate-resources</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-artifacts</id>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>${project.build.directory}/classes/${features.file}</file>
                                    <type>xml</type>
                                    <classifier>features</classifier>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skip>${skip.karaf}</skip>
                    <systemPropertyVariables>
                        <karaf.distro.groupId>org.opendaylight.controller</karaf.distro.groupId>
                        <karaf.distro.artifactId>opendaylight-karaf-empty</karaf.distro.artifactId>
                        <karaf.distro.version>${opendaylight-karaf-empty.version}</karaf.distro.version>
                    </systemPropertyVariables>
                    <!--<dependenciesToScan>
           <dependency>org.opendaylight.yangtools:features-test</dependency>
          </dependenciesToScan>-->
        </configuration>
    </plugin>
   </plugins>
  </build>    
</project>
